# Required: change this for production
SESSION_SECRET=change-me-to-a-random-string

# Optional overrides (defaults shown, set in Dockerfile/docker-compose)
# OLLAMA_HOST=http://ollama:11434
# HOST=0.0.0.0
# PORT=8000
# DATA_DIR=/app/data
# DB_DIR=/app/db