# Required: change this to a random string
SESSION_SECRET=change-me-to-a-random-string

# Optional overrides (defaults shown, set in Dockerfile/docker-compose)
# OLLAMA_HOST=http://ollama:11434
# HOST=0.0.0.0
# PORT=8000
# DATA_DIR=/app/data
# DB_DIR=/app/db
# COOKIE_SECURE=true   # set to true when behind HTTPS reverse proxy