ollama:
  host: http://localhost:11434

models:
  query: qwen2.5:7b
  enrich: gemma4:e4b
  structure: qwen2.5:7b
  vision: gemma3:4b

options:
  num_ctx: 16384

paths:
  data_dir: ./data
  db_dir: ./db

server:
  host: 0.0.0.0
  port: 8050
  secret: change-me-in-production

# All values above can be overridden by environment variables:
#   OLLAMA_HOST, SESSION_SECRET, DATA_DIR, DB_DIR, HOST, PORT, COOKIE_SECURE