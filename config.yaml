ollama:
  host: http://localhost:11434

models:
  query: qwen2.5:7b-instruct-q4
  enrich: gemma3:4b-it-q4
  structure: qwen2.5:7b-instruct-q4
  vision: gemma3:4b-it-q4

paths:
  data_dir: ./data
  db_dir: ./db

server:
  host: 0.0.0.0
  port: 8000
  secret: change-me-in-production

# All values above can be overridden by environment variables:
#   OLLAMA_HOST, SESSION_SECRET, DATA_DIR, DB_DIR, HOST, PORT